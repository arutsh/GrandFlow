# Vite requires VITE_ prefix for vars exposed to the browser.
# These are embedded at build time — changes require a rebuild/restart.
# All API traffic goes through nginx at :8082, which routes to the correct service.
VITE_API_GATEWAY=http://127.0.0.1:8082/api/v1
